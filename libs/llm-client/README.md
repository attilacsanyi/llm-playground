# llm-client

This library was generated with [Nx](https://nx.dev).

```bash
pnpm nx g @nx/node:library --directory=libs/llm-client --linter=eslint --name=llm-client --unitTestRunner=jest --importPath=@llm/llm-client --simpleModuleName=true --strict=true
```

## Building

Run `nx build llm-client` to build the library.

## Running unit tests

Run `nx test llm-client` to execute the unit tests via [Jest](https://jestjs.io).
