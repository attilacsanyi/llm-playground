# vector-store

This library was generated with [Nx](https://nx.dev).

```bash
pnpm nx g @nx/node:library --directory=libs/vector-store --linter=eslint --name=vector-store --unitTestRunner=jest --importPath=@llm/vector-store --simpleModuleName=true --strict=true
```

## Building

Run `nx build vector-store` to build the library.

## Running unit tests

Run `nx test vector-store` to execute the unit tests via [Jest](https://jestjs.io).
