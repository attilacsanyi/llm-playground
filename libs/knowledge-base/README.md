# knowledge-base

This library was generated with [Nx](https://nx.dev).

```bash
pnpm nx g @nx/node:library --directory=libs/knowledge-base --linter=eslint --name=knowledge-base --unitTestRunner=jest --importPath=@llm/knowledge-base --simpleModuleName=true --strict=true
```

## Building

Run `nx build knowledge-base` to build the library.

## Running unit tests

Run `nx test knowledge-base` to execute the unit tests via [Jest](https://jestjs.io).
